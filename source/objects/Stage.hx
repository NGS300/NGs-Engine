package objects;

class Stage extends BeatState {

    public var curStage = '';

    override function create() {
        super.create();
    }
    
    override function update(elapsed:Float) {
        super.update(elapsed);
    }

    
}